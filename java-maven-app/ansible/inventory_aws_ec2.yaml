---
plugin: aws_ec2
regions:
  - eu-north-1

# filters:
#   instance-state-name: running
  # tag:Name: dev*

keyed_groups:
  - key: tags
    prefix: tag
  - key: instance_type
    prefix: instance_type

